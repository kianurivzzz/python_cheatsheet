'''
-----------------------------------------------RU----------------------------------------------------------------
Переменные предназначены для хранения данных.
При выборе имён для переменных необходимо соблюдать следующие правила:
- Первым символом переменной должна быть буква из алфавита, а также символ нижнего подчёркивания _
- Остальная часть переменной может состоять из букв, знаков нижнего подчёркивания _ или
цифр (0-9).
- Регистр букв имеет значения. Например, myname и Myname – это не одно и то же.
Обратите внимание на 'M' в нижнем регистре в первом случае и 'N' в верхнем во втором.
- Примеры допустимых имён переменных: my_name123, _path, amount_students.
- Примеры недопустимых имён переменных: 2things, здесь есть пробелы,
my-name, >a1b2_c3 и 'это_в_кавычках'.

Переменные могут хранить значения любых типов, называемых типами данных.

------------------------------------------------EN---------------------------------------------------------------
Variables are used to store data.
The following rules must be followed when choosing names for variables:
- The first character of the identifier must be a letter from the alphabet, and the underscore character _
- The rest of the identifier can consist of letters, underscores _ or
numbers (0-9).
- The case of letters is important. For example, myname and Myname are not the same thing.
Note the 'M' in lowercase in the first case and 'N' in uppercase in the second.
- Examples of valid variable names: my_name123, _path, amount_students.
- Examples of invalid variable names: 2things, there are spaces here,
my-name, >a1b2_c3 and 'this_in_quotes'.

Variables can store values of any type, called data types.

'''

i = 5
print(i)

i = i + 1
print(i)

s = 'Я строка'
print(s)

# i, s - названия переменных/variable names

# print() - название функции. Названия функций обязательно записываются со скобками/function name.
#Function names must be written with parentheses

# py vars.py - запуск файла/file run

# 5 6 'Я строка' - результаты работы программы/program results
